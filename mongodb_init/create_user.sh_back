#!/bin/bash
# sudo chown -R mongodb:mongodb /home/mongodb

nohup gosu mongodb mongo ${MONGO_DB_NAME} --eval "db.createUser({ user: ${MONGO_INITDB_USER_USERNAME_FILE}, pwd: ${MONGO_INITDB_USER_PASSWORD_FILE}, roles: [{ role: 'readWrite', db: ${MONGO_DB_NAME} }, { role: 'read', db: 'local' }]});"

